set $alt Mod1
set $mod Mod4
set $ws1 "1:  "
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6:  "
set $ws7 "7  "
set $ws8 "8:  "
set $ws9 "9:  "
set $ws10 "10:  "
set $sep " | "
set $ac #33ff00
set $bg #000000cc
#[80]cc [70]b3 [60]99 [50]80
set $e exec --no-startup-id
exec_always --no-startup-id picom --experimental-backends
exec_always --no-startup-id feh --bg-fill $HOME/Pictures/wp
bindsym $alt+e $e exitmenu
bindsym $mod+Return $e konsole
bindsym $mod+r $e pkill -SIGRTMIN+5 i3blocks
bindsym $mod+d $e dmenu_run -nf '#33ff00' -nb '#000000' -sf '#000000' -sb '#33ff00' -fn 'noto sans:size=11' -i -b
bindsym $mod+Shift+d $e i3-dmenu-desktop --dmenu="dmenu -nf '#33ff00' -nb '#000000' -sf '#000000' -sb '#33ff00' -fn 'noto sans:size=11' -i -b"

bindsym $mod+q kill
bindsym $mod+f fullscreen toggle
bindsym $mod+c move position center
bindsym $mod+u [urgent=latest] focus
bindsym $mod+s split toggle
bindsym $mod+Shift+s layout toggle split
bindsym $mod+space focus mode_toggle
bindsym $mod+Shift+space floating toggle
floating_modifier $mod
bindsym $mod+i gaps inner current plus 5
bindsym $mod+o gaps outer current plus 5
bindsym $mod+Shift+i gaps inner current minus 5
bindsym $mod+Shift+o gaps outer current minus 5
bindsym $mod+Shift+r restart

bindsym $mod+1 workspace $ws1
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace $ws3
bindsym $mod+4 workspace $ws4
bindsym $mod+5 workspace $ws5
bindsym $mod+6 workspace $ws6 $e flatpak run com.jetbrains.PyCharm-Community
bindsym $mod+7 workspace $ws7 $e pidof code || code
bindsym $mod+8 workspace $ws8 $e pidof spotify || flatpak run com.spotify.Client
bindsym $mod+9 workspace $ws9 $e pidof dolphin || dolphin
bindsym $mod+0 workspace $ws10 $e pidof firefox || firefox
bindsym $mod+b workspace back_and_forth
bindsym $mod+n workspace prev
bindsym $mod+m workspace next
assign [class="jetbrains-pycharm-ce"] $ws6
assign [class="Code"] $ws7
for_window [class="Spotify"] move --no-auto-back-and-forth to workspace $ws8
assign [class="dolphin"] $ws9
assign [class="firefox"] $ws10

font pango:DejaVu Sans Mono 10
default_border pixel 0
default_floating_border pixel 1
for_window [window_role="pop-up"] floating enable
for_window [window_role="task_dialog"] floating enable
for_window [class="mpv" instance="gl"] floating enable, resize set height 1080, move position center
for_window [class="Blueman-manager" instance="blueman-manager"] floating enable, move position center

bar {
    position top
    tray_output none
    separator_symbol $sep
    status_command i3blocks
    workspace_min_width 30
    strip_workspace_numbers yes
    font pango:NotoSansNerdFont 10.5
    i3bar_command i3bar --transparency
    colors {
    separator $ac
    background $bg
    # class            border backgr. text
    focused_workspace  $bg    $bg     $ac
    inactive_workspace $bg    $bg     #7b7061
    urgent_workspace   $bg    $bg     #ff3300
    }
}

# Plasma compatibility improvements
for_window [title="Desktop — Plasma"] kill; floating enable; border none
for_window [title="plasma-desktop"] floating enable; border none
for_window [title="win7"] floating enable; border none
for_window [class="plasmashell"] floating enable
for_window [class="Plasma"] floating enable; border none
for_window [class="systemsettings"] floating enable
for_window [class="krunner"] floating enable; border none
for_window [class="Kmix"] floating enable; border none
for_window [class="Klipper"] floating enable; border none
for_window [class="Plasmoidviewer"] floating enable; border none
for_window [class="plasmashell" window_type="notification"] border none
for_window [class="plasmashell" window_type="dock"] border none
no_focus [class="plasmashell" window_type="notification"]
no_focus [class="plasmashell" window_type="dock"]

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+h move left 20
bindsym $mod+Shift+j move down 20
bindsym $mod+Shift+k move up 20
bindsym $mod+Shift+l move right 20
bindsym $mod+Shift+Left move left 20
bindsym $mod+Shift+Down move down 20
bindsym $mod+Shift+Up move up 20
bindsym $mod+Shift+Right move right 20

# resize focused window
bindsym $mod+$alt+h resize shrink width 20
bindsym $mod+$alt+j resize shrink height 20
bindsym $mod+$alt+k resize grow height 20
bindsym $mod+$alt+l resize grow width 20
bindsym $mod+$alt+Left resize shrink width 20
bindsym $mod+$alt+Down resize shrink height 20
bindsym $mod+$alt+Up resize grow height 20
bindsym $mod+$alt+Right resize grow width 20

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $ws1
bindsym $mod+Shift+2 move container to workspace $ws2
bindsym $mod+Shift+3 move container to workspace $ws3
bindsym $mod+Shift+4 move container to workspace $ws4
bindsym $mod+Shift+5 move container to workspace $ws5
bindsym $mod+Shift+6 move container to workspace $ws6
bindsym $mod+Shift+7 move container to workspace $ws7
bindsym $mod+Shift+8 move container to workspace $ws8
bindsym $mod+Shift+9 move container to workspace $ws9
bindsym $mod+Shift+0 move container to workspace $ws10
bindsym $mod+Shift+b move container to workspace back_and_forth, workspace back_and_forth
bindsym $mod+Shift+n move container to workspace prev, workspace prev
bindsym $mod+Shift+m move container to workspace next, workspace next

# Use pactl to adjust volume in PulseAudio.
set $sig10 pkill -SIGRTMIN+10 i3blocks
bindsym XF86AudioRaiseVolume $e pactl set-sink-volume @DEFAULT_SINK@ +10% && $sig10
bindsym XF86AudioLowerVolume $e pactl set-sink-volume @DEFAULT_SINK@ -10% && $sig10
bindsym XF86AudioMute $e pactl set-sink-mute @DEFAULT_SINK@ toggle && $sig10
bindsym XF86AudioMicMute $e pactl set-source-mute @DEFAULT_SOURCE@ toggle && $sig10
